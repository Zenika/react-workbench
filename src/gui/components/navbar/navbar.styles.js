import { css } from 'glamor'

const layout = css({
  display: 'flex',
  alignItems: 'center',
})

const title = css({
  fontSize: '1.2em',
  fontWeight: 'bolder',
  paddingLeft: '10px',
})

export default { layout, title }
