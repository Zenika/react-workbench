import { css } from 'glamor'

const layout = css({
  height: '100vh',
  display: 'flex',
  flexDirection: 'column',
})

const main = css({
  height: '100%',
  overflow: 'hidden',
})

export default { layout, main }
