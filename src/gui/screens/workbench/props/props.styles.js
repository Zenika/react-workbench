import { css } from 'glamor'

const layout = css({
  display: 'block',
  boxSizing: 'border-box',
})

const title = css({
  fontWeight: 'bold',
  fontSize: '1.1em',
  paddingBottom: '.5em',
})

export default { layout, title }
