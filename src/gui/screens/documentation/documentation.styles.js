import { css } from 'glamor'

const layout = css({
})

const edit = css({
  height: '100%',
  width: '100%',
  maxHeight: '100%',
  outline: 'none',
  border: '0px',
})

export default { layout, edit }
